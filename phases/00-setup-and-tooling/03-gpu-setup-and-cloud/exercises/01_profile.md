# Exercise 1 — Hardware profile

Outcome recorded in `../notes.md` under **Hardware profile**.

The exercise was: capture a structured comparison of the local machine vs the Colab T4, using observed values from `verify.py`, `gpu_check.py`, and `nvidia-smi`.
